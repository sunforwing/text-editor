# 数据库

### 1. 基础与用户模块

此模块定义了系统中最基础的实体，包括用户、网点/中转站和车辆。

#### **`User` (系统用户表)**

- **用途:** 存储系统内各类用户的基本信息和角色。
- **对应角色:** `staff_station` (网点员工), `dispatcher` (调度员), `driver` (司机), `courier` (快递员), `admin` (管理员)。
- **关键字段:**
  - `id` (Primary Key, Index)
  - `username` (Unique, Index): 用户名。
  - `role`: 用户角色。
  - `station_id` (Nullable): 逻辑关联到 `Station` 表，表示用户所属的网点/中转站。
  - `phone`: 联系电话。

#### **`Station` (网点/中转站)**

- **用途:** 存储物流网络中的各个处理节点（如营业网点、分拨中心）。
- **关键字段:**
  - `id` (Primary Key, Index)
  - `name`: 站点名称。
  - `type`: 站点类型，例如 `outlet` (营业网点), `hub` (分拨中心/中转站)。
  - `geo_location`: 地理坐标，存储为 JSON 格式，包含 `{lat: ..., lng: ...}`。

#### **`Vehicle` (车辆信息)**

- **用途:** 存储用于运输的车辆信息。
- **关键字段:**
  - `id` (Primary Key, Index)
  - `plate_number`: 车牌号。
  - `capacity`: 载重能力 (Float)。
  - `status`: 车辆状态，例如 `idle` (空闲), `busy` (忙碌), `maintenance` (维护)。

------

### 2. 包裹核心模块

此模块是系统的核心，定义了包裹的主信息、路由规划和生命周期日志。

#### **`Parcel` (包裹主表)**

- **用途:** 存储包裹（运单）的所有核心信息。
- **关键字段:**
  - `tracking_number` (Primary Key, Index): **运单号**，作为主键。
  - `sender_info`, `receiver_info`: 发/收件人信息，存储为 JSON 格式，包含 `{name, phone, address}`。
  - `weight`, `volume`: 包裹的重量和体积。
  - `status`: 包裹当前状态，默认为 `created`。状态有 ["created", "sorting", "transporting", "dispatching", "delivered"]
  - `current_station_id`, `next_station_id`: 当前和下一个处理站点的 ID (逻辑关联 `Station.id`)。
  - `start_station_id`, `final_station_id`: 始发站和最终目的地的站点 ID (逻辑关联 `Station.id`)。

#### **`ParcelRoute` (包裹规划路径)**

- **用途:** 存储包裹从始发地到目的地经过的规划站点序列。
- **关键字段:**
  - `id` (Primary Key, Index)
  - `parcel_id` (Unique, Index): 逻辑关联 `Parcel.tracking_number`。
  - `route_sequence`: 规划的路径序列，存储为 JSON 数组，例如 `[StationID_1, StationID_2, ...]`。

#### **`ParcelLog` (包裹全生命周期轨迹日志)**

- **用途:** 记录包裹在物流过程中每一步操作的详细轨迹。
- **关键字段:**
  - `id` (Primary Key, Index)
  - `parcel_id` (Index): 逻辑关联 `Parcel.tracking_number`。
  - `station_id`: 发生操作的站点 ID (逻辑关联 `Station.id`)。
  - `operator_id`: 执行操作的用户 ID (逻辑关联 `User.id`)。
  - `action`: 执行的操作类型。状态有PARCEL_LOG_ACTIONS = [
        "created", "sorting_started","sorting_completed",
       "transport_started","transport_completed","dispatch_started", "dispatch_completed"]
  - `description`: 操作的详细描述。

------

### 3. 运输与调度模块

此模块管理包裹在网点之间的中长途运输任务。

#### **`TransportTask` (干线/支线运输任务)**

- **用途:** 定义一次具体的长途/短途运输任务。
- **关键字段:**
  - `id` (Primary Key, Index)
  - `task_code` (Index): 任务编号。
  - `driver_id`: 负责该任务的司机 ID (逻辑关联 `User.id`)。
  - `vehicle_id`: 使用的车辆 ID (逻辑关联 `Vehicle.id`)。
  - `start_station_id`, `end_station_id`: 运输的起/止站点 ID (逻辑关联 `Station.id`)。
  - `status`: 任务状态， `planned` (已计划), `in_transit` (运输中), `completed` (已完成)。TRANSPORT_TASK_STATUSES = ["planned", "in_transit", "completed", "cancelled"]

#### **`TaskParcelRelation` (任务-包裹关联表)**

- **用途:** 实现 **`TransportTask`** 和 **`Parcel`** 之间的多对多关系，记录一个任务中装载了哪些包裹。
- **关键字段:**
  - `task_id` (Primary Key): 逻辑关联 `TransportTask.id`。
  - `parcel_id` (Primary Key): 逻辑关联 `Parcel.tracking_number`。

------

### 4. 末端派送模块

此模块专注于包裹到达末端网点后的最后一公里派送流程。

#### **`DeliveryTask` (最后一公里派送任务)**

- **用途:** 存储包裹从末端网点到收件人的派送任务信息。

- **关键字段:**
  - `id` (Primary Key, Index)
  
  - `courier_id` (Index): 负责派送的快递员 ID (逻辑关联 `User.id`)。
  
  - `parcel_id` (Index): 正在派送的包裹 ID (逻辑关联 `Parcel.tracking_number`)。
  
  - `status`: 派送状态，如 `assigned` (已分配), `delivering` (派送中), `success` (成功), `fail` (失败)。
  
    DELIVERY_TASK_STATUSES = ["assigned", "delivering", "success", "fail"] 
  
  - `fail_reason` (Nullable): 派送失败的原因。

------

# API接口定义

### 1. 创建运单

- **URL:** `POST /api/v1/parcels`

- **Request Body:**

  JSON

  ```
  {
    "sender": {"name": "张三", "phone": "138...", "address": "..."},
    "receiver": {"name": "李四", "phone": "139...", "address": "..."},
    "weight": 2.5,
    "volume": "10x10x10",
    "start_station_id": 1,
    "final_station_id": 1,
  }
  ```

- **Response:**

  JSON

  ```
  {
    "tracking_number": "SF12345678",
    "status": "created",
  }
  ```

**描述:** 

先在parcel_router表里面新增条目

判断next_station_id是否和final_station_id相等。报一个错。

如果不想等，parcel表状态改为created，写入到parcel_load表 中的action为created

这里的current_station_id为start_station_id

Next_station_di为calculate_next_station(current_station_id)



### 2. 查询包裹轨迹 (全生命周期跟踪)

- **URL:** `GET /api/v1/parcels/trace?tracking_number=

- **prams**： ?tracking_number：xxxxxx

- **Response:**

  JSON

  ```
  {
    "tracking_number": "SF12345678",
    "sender": {"name": "张三", "phone": "138...", "address": "..."},
    "receiver": {"name": "李四", "phone": "139...", "address": "..."},
    "weight": 2.5,
    "volume": "10x10x10",
    "start_station_id": 1,
    "final_station_id": 1,
    "current_station_id": 1,
    "next_station_id":1,
    "logs": [
      {"time": "2025-11-30 09:00", "station": "A网点", "action": "pickup", "desc": "已揽收"},
      {"time": "2025-11-30 20:00", "station": "上海分拣中心", "action": "load", "desc": "分拣完成，发往北京"}
    ]
  }
  ```

- **描述:** 获取包裹详情及完整的日志列表。

  

### 3.状态更新

- **URL:** `POST /api/v1/parcels/scan`

- **Request Body:**

  JSON

  ```
  {
  	"tracking_number":xxxxx,
  	"action":sort/transport/dispath/finish,
  }
  ```
  
- **描述:**  

  (1) "action":sort 时候，校验之前的status为created或者dispathing

  status状态转移：
  
  1、created->sorting , 写入log里面一个action为sort_started
  
  current_station_id 和 next_station_id 不变
  
  2、dispathing->sorting, 写入log里面两个条目，一个action为dispatch_completed，另外一个action为sort_started
  
  current_station_id 和 next_station_id 不变
  
  
  
  (2)"action":transport 时候，校验之前的status为sorting
  
  status状态转移：sorting->transporting 写入log里面两条条目，一个action为sorted_completed，还有一个为transport_started
  
  current_station_id = 原 next_station_id
  
  next_station_id = calculate_next_station(current_station_id)
  
  
  
  (3)"action":dispath 时候，校验之前的status为sorting
  
  status状态转移：sorting->dispathing 写入log里面两条条目，一个action为sorted_completed，还有一个为dispathed_started
  
  current_station_id = next_station_id（已到达目的地派送站）
  
  next_station_id = None
  
  
  
  (4)"action":finish时候，校验之前的status为dispathing
  
  status状态转移：dispathing->delivered, 写入log里面一个条目action为dispatch_completed
  
  current_station_id = next_station_id（已到达目的地派送站）
  
  next_station_id = None
  
  ----------
  
  
  
  def calculate_next_station(current_station_id: int,):
  
  ​    """
  
  ​    模拟路径规划算法
  
  ​    实际项目中应基于图算法 (Dijkstra) 或查询数据库中的路由规则表
  
  ​    """
  
  ​    \# 简单 Mock: 如果当前是 1(上海)，下一站去 2(上海分拨)
  
  ​    if current_station_id == 1:
  
  ​        return 2
  
  ​    \# 如果当前是 2(上海分拨)，下一站去 3(北京分拨)
  
  ​    if current_station_id == 2:
  
  ​        return 3
  
  ​    return None

### 4.创建运输任务

- **URL:** `POST /api/v1/transport/start`

- **Request Body:**

  JSON

  ```
  {
    "driver_id": 1,
    "vehicle_id": 3,
    "parcel_ids": ["SF123...", "SF456..."] // 装车的包裹列表
  }
  ```

- **描述:**  

  这里会在新增一个transport_tasks的条目，且status为planned

  这里的会自动创建task_code，写到parcel_task_relation里面task_code 和 parcel_id 一一对应

### 5.更新运输状态 (发车/到达)

- **URL:** `PUT /api/v1/transport/status`

- **Request Body:**

  JSON

  ```
  {
    “task_code”:"xxxxx"
    "status": "completed", // in_transit 或 completed
  }
  ```

  更新transport_tasks的表的相应的status

  (1) 如果是in_transit，按照上面的POST /api/v1/parcels/scan 的函数的逻辑，

  parcel表status状态转移：sorting->transporting 写入log里面两条条目，一个action为sorted_completed，还有一个为transport_started

  current_station_id = 原 next_station_id

  next_station_id = calculate_next_station(current_station_id),

  

  (2) 如果是completed，按照上面的POST /api/v1/parcels/scan 的函数的逻辑，

  parcel表status 从dispathing->sorting, 写入log里面两个条目，一个action为dispatch_completed，另外一个action为sort_started

  current_station_id 和 next_station_id 不变

  

  ### 6.创建最后一公里送达任务
  
  - **URL:** `POST /api/v1/delivery/start`
  
  - **Request Body:**
  
    ```
    {
    		"courier_id":3
    		"parcel_ids":["xxx","xxxx"]
    }
    ```
  
  - 描述：
  
    先校验是否parcel表的这个条目状态是否为sorting，如果不是报错包裹不在物流
  
    再校验这个条目的currend_station_id 和 final_station_id 是否一致，如果不一致报错包裹未达到末尾站
  
    如果是的，将其parcel的状态改为dispatching
  
    写入log里面两条条目，一个action为sorted_completed，还有一个为dispathed_started
  
    然后为分别每个parcel写入delivery的status为assigned状态, 写入时间
  
  ### 7、更新送达状态
  
  - **URL:** `PUT /api/v1/delivery/status`
  
  - **Request Body:**
  
    JSON
  
    ```
    {
      “parcel_id”:1
      "status": "delivering", // delivering 或 success
    }
    ```
  
    这里如果status的状态为delivering，则代表这个包裹成功被快递小哥接受了，就需要修改一下delivery_tasks表的条目的状态就行了
  
    这里如果status的状态success，则代表这个包裹被签收了，需要修改一下delivery_tasks表的条目的状态和完成的时间，再修改parcel的状态delivered，最后在pacel_log新增加一个action为dispatch_completed的条目
  
  
  
  
  
  

