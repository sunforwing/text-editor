# 快递物流管理系统 - 后端技术规格说明书 

## 1. 系统架构概述

本阶段核心目标是打通“揽收-分拣-运输-派送”的全链路。

- **后端 (Python):** 负责业务逻辑、路径规划算法、数据持久化。
- **前端 (Vue):** 负责网点录入、调度员大屏、司机/派送员移动端界面展示。

------

## 2. 数据库设计 (Database Schema)

### 2.1 基础与用户模块

User (系统用户)

用于网点人员、调度员、司机、派送员的登录。

- `id`: 主键 (Integer)
- `username`: 用户名 (String)
- `password_hash`: 密码哈希 (String)
- `role`: 角色 (Enum: staff_station, dispatcher, driver, courier, admin)
- `station_id`: 所属网点ID (ForeignKey, 可空)
- `phone`: 联系电话 (String)

Station (网点/中转站)

物流网络中的节点。

- `id`: 主键 (Integer)
- `name`: 网点名称 (String, 如"上海分拣中心")
- `type`: 类型 (Enum: outlet-网点, hub-中转中心)
- `address`: 详细地址 (String)
- `geo_location`: 经纬度 (JSON/Point, 用于路径规划)

### 2.2 包裹核心模块

Parcel (包裹)

系统的核心实体，记录包裹的基础信息和当前流转状态。

- `tracking_number`: 主键/唯一索引 (String, 运单号)
- `sender_info`: 发件人信息 (JSON: {name, phone, address})
- `receiver_info`: 收件人信息 (JSON: {name, phone, address})
- `weight`: 重量 (Float)
- `volume`: 体积/尺寸 (String)
- `status`: 当前状态 (Enum: created, sorting, in_transit, dispatching, delivered, exception)
- `current_station_id`: 当前所在网点ID (ForeignKey, 运输中则为空或指向上一站)
- `next_station_id`: 计划前往的下一站ID (ForeignKey, 由路径算法计算)
- `created_at`: 创建时间 (DateTime)

ParcelRoute (包裹规划路径)

记录系统为包裹规划的静态路线（可选，用于比对实际路线）。

- `id`: 主键
- `parcel_id`: 关联包裹
- `route_sequence`: 路线序列 (JSON List: [StationA_ID, StationB_ID, StationC_ID])

ParcelLog (包裹轨迹/全生命周期日志)

用于前端展示“历史轨迹追踪”。

- `id`: 主键
- `parcel_id`: 关联包裹 (ForeignKey)
- `station_id`: 操作发生的网点 (ForeignKey)
- `operator_id`: 操作员ID (ForeignKey)
- `action`: 动作 (Enum: pickup, load, unload, sort, delivery_start, sign_off, exception)
- `description`: 描述文本 (String, 如"到达上海分拣中心")
- `created_at`: 时间戳 (DateTime)

### 2.3 运输与调度模块

**Vehicle (车辆)**

- `id`: 主键
- `plate_number`: 车牌号 (String)
- `capacity`: 载重能力 (Float)
- `status`: 状态 (Enum: idle, busy, maintenance)

TransportTask (运输任务)

调度员创建的干线/支线运输任务。

- `id`: 主键
- `task_code`: 任务编号 (String, 如 "T-SH-BJ-20251201-01")
- `driver_id`: 司机ID (ForeignKey)
- `vehicle_id`: 车辆ID (ForeignKey)
- `start_station_id`: 起始站
- `end_station_id`: 目的站
- `status`: 状态 (Enum: planned, in_transit, completed, cancelled)
- `start_time`: 发车时间
- `end_time`: 到达时间

TaskParcelRelation (任务-包裹关联表)

记录某次运输任务装载了哪些包裹。

- `task_id`: 运输任务ID
- `parcel_id`: 包裹ID

### 2.4 末端派送模块

DeliveryTask (派送任务)

最后一公里派送。

- `id`: 主键
- `courier_id`: 派送员ID (User ForeignKey)
- `parcel_id`: 包裹ID (OneToOne)
- `status`: 派送状态 (Enum: assigned, delivering, success, fail)
- `fail_reason`: 失败/异常原因 (String, 仅在异常时有值)
- `assigned_at`: 分配时间

------

## 3. API 接口设计 (REST API)

以下接口仅列出核心业务逻辑，省略了基础的 CRUD 和鉴权 Header。

### 3.1 揽收与基础查询 (客户端/网点端)

**1. 创建运单 (网点录入)**

- **URL:** `POST /parcels`

- **描述:** 网点工作人员录入信息，系统内部触发路径规划，计算 `next_station_id`。

- **Request Body:**

  JSON

  ```
  {
    "sender": {"name": "张三", "phone": "138...", "address": "..."},
    "receiver": {"name": "李四", "phone": "139...", "address": "..."},
    "weight": 2.5,
    "volume": "10x10x10",
    "start_station_id": 1
  }
  ```

- **Response:**

  JSON

  ```
  {
    "tracking_number": "SF12345678",
    "status": "created",
    "next_station": "上海分拣中心"
  }
  ```

**2. 查询包裹轨迹 (全生命周期跟踪)**

- **URL:** `GET /api/v1/parcels/{tracking_number}/trace`

- **描述:** 获取包裹详情及完整的日志列表。

- **Response:**

  JSON

  ```
  {
    "tracking_number": "SF12345678",
    "current_status": "in_transit",
    "current_location": "京沪高速",
    "estimated_arrival": "2025-12-01 10:00",
    "logs": [
      {"time": "2025-11-30 09:00", "station": "A网点", "action": "pickup", "desc": "已揽收"},
      {"time": "2025-11-30 20:00", "station": "上海分拣中心", "action": "sort", "desc": "分拣完成，发往北京"}
    ]
  }
  ```

### 3.2 分拣与中转管理 (分拣中心端)

**3. 包裹扫描/状态更新**

- **URL:** `POST /api/v1/parcels/{tracking_number}/scan`

- **描述:** 包裹到达中转站或网点时扫描，更新位置，记录日志。

- **Request Body:**

  JSON

  ```
  {
    "station_id": 5,
    "action": "arrive", // 动作：arrive(到达), sort(分拣), exception(异常)
    "description": "到达北京分拣中心"
  }
  ```

- **逻辑说明:** 若 action 为 `sort`，系统会根据路径规划返回该包裹的下一站 ID，提示分拣员放入哪个笼车。

**4. 上报异常 (扩展流程)**

- **URL:** `POST /api/v1/parcels/{tracking_number}/exception`

- **Request Body:**

  JSON

  ```
  {
    "exception_type": "address_error", // 地址不清/破损/丢件
    "description": "收件人电话空号",
    "station_id": 5
  }
  ```

### 3.3 运输调度 (调度员/司机端)

**5. 创建运输任务 (调度)**

- **URL:** `POST /api/v1/transport-tasks`

- **描述:** 调度员指派车辆和司机，规划路线。

- **Request Body:**

  JSON

  ```
  {
    "driver_id": 101,
    "vehicle_id": 202,
    "start_station_id": 5,
    "end_station_id": 8,
    "parcel_ids": ["SF123...", "SF456..."] // 装车的包裹列表
  }
  ```

**6. 获取司机任务列表 (司机端)**

- **URL:** `GET /api/v1/driver/tasks`

- **Query Params:** `?status=planned` (获取待执行任务)

- **Response:**

  JSON

  ```
  [
    {
      "task_id": 5001,
      "route": "上海 -> 北京",
      "parcel_count": 85,
      "vehicle_plate": "沪A88888"
    }
  ]
  ```

**7. 更新运输状态 (发车/到达)**

- **URL:** `PUT /api/v1/transport-tasks/{task_id}/status`

- **Request Body:**

  JSON

  ```
  {
    "status": "completed", // in_transit 或 completed
    "current_station_id": 8 // 仅在到达时需要
  }
  ```

  *逻辑说明:* 任务完成时，后端自动批量更新该任务下所有包裹的位置为目的站点，并记录日志。

### 3.4 末端派送 (快递员端)

**8. 分配派送任务**

- **URL:** `POST /api/v1/delivery-tasks/assign`
- **描述:** 包裹到达终点网点后，系统或人工分配给快递员。

**9. 获取我的派送列表 (快递员端)**

- **URL:** `GET /api/v1/courier/my-tasks`

- **Response:**

  JSON

  ```
  [
    {
      "tracking_number": "SF123...",
      "receiver_name": "李四",
      "address": "朝阳区xx路...",
      "phone": "139...",
      "status": "assigned"
    }
  ]
  ```

**10. 签收或反馈异常**

- **URL:** `POST /api/v1/delivery-tasks/{tracking_number}/result`

- **Request Body:**

  JSON

  ```
  {
    "result": "success", // success 或 fail
    "signer": "本人", // 签收人
    "fail_reason": null,
    "geo_location": "..." // 签收时的定位
  }
  ```

------

## 4. 关键业务逻辑处理说明

### 4.1 路径规划 (后端 Python 实现)

- 在调用“创建运单”或“分拣扫描”时，后端服务需要运行路径算法（如 Dijkstra 或 基于规则的配置表）。
- **逻辑:** 输入（当前站点，目的地址） -> 输出（下一跳站点 ID）。
- **异常处理:** 若发生路线变更（如天气原因），管理员调用 `POST /api/v1/routes/replan` 接口，后端更新受影响包裹的 `next_station_id`。

### 4.2 状态流转机

包裹状态流转必须严格遵守顺序，建议在 Model 层添加验证：

- `created` -> `sorting` (网点)
- `sorting` -> `dispatching` (等待装车)
- `dispatching` -> `in_transit` (运输中)
- `in_transit` -> `sorting` (到达中转站，循环)
- `sorting` -> `delivering` (到达目的网点，分配小哥)
- `delivering` -> `delivered` (签收)

### 4.3 异常处理机制

- **分拣异常:** 包裹置为 `exception` 状态，需提供接口供客服修改地址或退回。修改后状态重置为 `sorting`。
- **派送异常:** 派送失败后，包裹状态回退至 `sorting` (待再次分配) 或标记为 `holding` (留仓)。

------

## 5. 建议的下一步行动

**Would you like me to:**

1. **为这些 API 编写具体的 Python (FastAPI 或 Flask) 代码框架？**
2. **详细设计 Vue 前端的组件结构（如：运单录入表单、司机任务卡片）？**
3. **补充关于“路径规划算法”的具体实现逻辑（如基于图的简单的节点查找）？**